 import {Header} from "../Components/Header"
 import { useMoviePlaying } from "../hooks/useMoviePlaying"
 export const Browse=()=>{
    useMoviePlaying();
    return (
        <>
        <Header></Header>
            </>
        
    )
}